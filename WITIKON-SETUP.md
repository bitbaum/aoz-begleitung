# Real apartment setup — superseded

created_date: 2026-01-24
last_modified_date: 2026-09-25
last_modified_summary: Reduced to a pointer. The repo is public, and it must not name a real flat or the people in it.

Setting up a real apartment: see `scripts/db/seed-real.ts` (with `--wipe` to
convert a demo instance in place). The apartment data itself (layout, and who
lives where) is provisioning config and does not belong in documentation. Login
codes are generated at runtime and printed once; they are never committed.

Where the product runs: [docs/INFRASTRUCTURE.md](docs/INFRASTRUCTURE.md).
